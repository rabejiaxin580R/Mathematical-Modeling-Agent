fastapi>=0.115.0
uvicorn[standard]>=0.30.0
openai>=1.50.0
python-dotenv>=1.0.0
jieba>=0.42.1
pydantic>=2.10.0
pandas>=2.0.0
openpyxl>=3.1.0
python-multipart>=0.0.9
markitdown[all]>=0.0.1a3
python-docx>=1.1.0
# 公式导出链路：LaTeX → MathML → OMML(Word 可编辑公式)。纯 Python，不需要 pandoc。
# 注意 mathml2omml 停更于 2019，export_docx._repair_omml 修了它的 groupChr 闭标签 bug。
latex2mathml>=3.77.0
mathml2omml>=0.1.0
# 科学计算栈：run_python / IDE 跑模型生成的建模代码所需。
# matplotlib 是核心功能（executor 自动采集其生成的图）；numpy 随 pandas 已装，此处显式钉住。
numpy>=1.26.0
matplotlib>=3.8.0
scipy>=1.11.0
sympy>=1.12
# 文献检索：arXiv 官方客户端（search_literature 使用）
arxiv>=2.1.0
