# 更新日志

## v2.2.0 (2026-09-06)

### 🆕 新功能：论文生成

**一键生成完整建模论文**——从大纲到全文，自动配图 + 文献溯源：

- 📝 **大纲 → 全文**：先确认大纲（章节自动推导），再逐章生成全文，全程 SSE 流式输出，支持续传 / 重生成
- 🔍 **在线文献检索**：检索服务迁到网关（鉴权不计费、结果缓存 7 天），agent 端网关优先、直连兜底
- 📊 **自动配图**：从数据表格自动选型成图（柱状 / 折线），base64 注入正文
- 📄 **导出 Word**：生成结果一键导出 .docx
- 🎭 **双风格等待界面**：剧情 / 终端两种日志风格可切换，章节进度条 + 心跳 + 卡住提示

### ✨ 改进

- 「做建模」入口新增「论文生成」卡片
- 完整论文页 UI 统一新中式配色
- 文献检索兜底：直连搜索线程化 + 20s 超时、重试降为 1 次，避免阻塞生成链路

### 🔧 技术

- 新增 paper_gen / paper_db / paper_literature / paper_viz 等论文生成模块，outline / paper 前端
- 网关新增 /v1/literature/search（鉴权不计费、7 天缓存）
- 清理无用测试脚本，.gitignore 补齐运行时产物

---

## v2.1.0 (2026-07-07)

### 🐛 修复：AI 回答被"吞掉"的严重 Bug

流式回答有时会在用户眼前消失——深入排查后发现 **6 个独立根因**，全部修复：

| # | 根因 | 修复 |
|---|------|------|
| ① | SSE 缓冲区在流结束时未 flush，最后的 `done`/`saved` 事件可能丢失 | 流结束后 `decoder.decode()` 强制 flush + 补处理残留 `sseBuf` |
| ② | 网络错误时 `innerHTML =` **覆盖**已渲染的流式内容 | 改为 `innerHTML +=` **追加**错误信息 |
| ③ | `finally` 块中调 `openConversation()` → `innerHTML = ""` **清空整个消息区**再重建，竞态或保存失败导致流式内容永久丢失 | 不再清空重建 DOM，改为在已有 DOM 上补回溯按钮和重新生成按钮 |
| ④ | `for chunk in stream` 循环不在 try 内，LLM 流中断时抛异常→无 `done` 事件→前端永远等待 | 用 try/except 包住，流中断时优雅截断并返回已有内容 |
| ⑤ | `handleEvent(ev)` 无 try/catch，一次 KaTeX 渲染失败就摧毁整个 SSE 流 | 加 try/catch 隔离 |
| ⑥ | `done` 事件 content 为空时覆盖了 token 增量渲染的既有内容 | 仅非空时覆盖 |

### ✨ 新功能：知识库按评级分层展示

知识库检索结果现在会根据用户的能力评级（L1~L5）**自动调整展示顺序和侧重点**：

- **L1/L2 萌新/入门**：优先展示「💡 新手入口」—— 一句话总结 + 直观理解 + 举个例子，公式仅保留核心并附白话解释
- **L3 进阶**：保持平衡排序（原有行为）
- **L4/L5 熟练/高手**：优先展示「🔬 核心内容」—— 数学原理 + 公式 + 步骤，例子放末尾参考
- `level` 参数从 HTTP 请求 → Agent → 工具调度 → 知识库检索 **全链路贯通**

### ✨ 新功能：首页评级引导弹窗

- 注册后进入主页时，若用户尚未完成评级（且未主动跳过），弹出引导弹窗
- 两个操作：「去测评 / 选等级」→ 跳转评估页；「稍后再说」→ 调用 skip API，永不再弹
- 点 × 关闭但不 skip，下次访问主页还会提醒

### ✨ 改进

- 角色/评级独立性澄清：`style_directive` 头部明确区分「**难度深度**由评级 L1~L5 决定」和「**内容侧重**由分工偏好决定」
- 输入框上方侧重按钮 "跟随评级" → "自动"（实际跟随的是角色偏好，旧标签有误导性）

### 🔧 技术

- `KnowledgeUnit.to_context()` 新增 `level` 参数，按等级重组段落排序
- `dispatch_tool` / `_do_search` 新增 `level` 参数，支持分层检索
- `stream_reply` / `_run_chat_stream` 新增 `level` 参数，全链路透传
- `handleEvent` 加 try/catch 隔离，防止单次渲染错误摧毁整个 SSE 流

---

## v2.0.0 (2026-06-26)

### 🆕 新功能

#### 📝 入门测评系统
- **55 道精选测评题**，覆盖 10 大知识模块（论文写作、新手入门、建模方法、求解算法、数据预处理、竞赛策略、可视化、编程工具、模型验证、数学基础）
- **3 个难度等级**（初级 / 中级 / 高级），自适应加权评分
- 做完自动**定级 L1~L5**，清晰告诉你当前水平
- **弱项分析**：哪些模块得分低一目了然，不用盲目瞎逛
- **推荐学习路径**：根据定级结果自动生成学习路线图（每级 3~4 个里程碑）
- 支持「做题定级」和「自己选级」两种模式

#### 🎁 免费 Token！我们搭了一个 API 网关

> **这是 v2.0 最大的亮点——你不用花一分钱就能用上大模型！**

我们部署了独立的 API 计费网关（`https://math-modeling.top`），专门给数学建模助教的用户提供免费额度：

- 🆓 **新用户注册即送 150 万 Token**，无需绑卡、无需付费
- 📧 邮箱注册 + 验证码，30 秒搞定
- 🔑 注册后在控制台一键生成 API Key（`sk-xxx`），粘贴到助教即可使用
- 💰 额度用完后可联系管理员免费兑换卡密续期
- 🔒 你的 API Key 直连网关，上游密钥不暴露，安全有保障

> 助教启动向导里选「用我们提供的免费额度」→ 打开控制台注册 → 领 Token → 生成 Key → 开用。详见 [使用指南](使用指南.md)。

### ✨ 改进
- macOS 用户指南新增 **iCloud 警告**（重要！放在 iCloud 会导致程序无法运行）
- 使用指南新增详细注册流程说明
- CI 构建修复：解决中文路径编码问题

### 🔧 技术
- 构建脚本添加 `PYTHONUTF8=1` 修复 Windows GitHub Actions 编码崩溃
- Release workflow 支持手动触发自动创建 tag 并发版

---

## v1.0.0 (2026-06-25)

- 🎉 初始发布
- 数学建模助教桌面版：Windows / macOS / Linux 三平台
- 四大核心功能：学习模式、做建模、真题练习、AI 对话
- 基于 40 小时数学建模课程知识库（BM25 检索，252 个知识单元）
- 自带 Python 运行环境，用户无需安装任何东西
- 支持连接自有 API Key（DeepSeek / 通义千问 / OpenAI / Moonshot）
